# Session Journal — 2026-08-08

> Chronological record: what was attempted, changed, succeeded/failed, commands, decisions, commits, next actions. Timestamps local (Asia/Kolkata). Today's session covers: MemoryOS bootstrap + continuity checkpoint.

## 2026-08-08

### 13:17 — Pre-session state (from prior chats, verified now)
- Working env: Windows 11, PowerShell 5.1, git 2.x, node v24.14.0.
- Two source repos exist:
  - `D:\Abhii\Opencode` (unversioned, not a git repo) — contains the NEW D1 (reconstruction md + PDFs) and `.genesis` scaffold (kit output).
  - `D:\Abhii\Projects\Conversational-Memory-Intelligence-System-` (old course repo — **READ-ONLY**, 3 commits) — contains 4 research weeks, naive baseline (NEVER RUN), + initialized genesis.
- Environment facts: Python 3.11 default `python` (no pypdf/markdown-it) vs Python 3.14 `pythoncore-3.14-64` (has both); Chrome 151; Git Bash at `C:\Program Files\Git\bin\bash.exe`.

### 13:30 — Task defined
User: create new repo `MemoryOS` per locked 7-phase plan (Phases 0–7, see PLAN.md). Plan approved; execution began.

### 13:40 — Discovery (correcting plan assumptions)
- **`D:\Abhii\Projects\MemoryOS` ALREADY EXISTS** — empty 10-dir skeleton, `git init`'d on `main`, 0 commits (user created it). No README/AGENTS yet.
- **`D:\Abhii\Opencode` is NOT a git repository** — assumption "commits in Opencode" was false; must copy sources only.
- Updated plan: Phase 0 = finalize skeleton files + continuity checkpoint (this commit). Original "mkdir/git init" reduced to verify-only.

### 13:35 — IMP.md checkpoint workflow started
- Read `D:\Abhii\Projects\IMP.md` (614 lines): 15-step context-continuity protocol (SESSION_STATE → PROJECT_MEMORY → CURRENT → DECISIONS → journal → PLAN → RESUME → AGENTS → checkpoint rule → commit → verify → report).
- Decisions confirmed by user: checkpoint hosts in MemoryOS; **single commit**; **full plan** in PLAN.md.

### 13:40 — Wrote continuity files (this responsibility):  details and cross-check status
Created:
- `docs/SESSION_STATE.md` — canonical state (this file).
- `docs/PROJECT_MEMORY.md` — durable knowledge.
- `CURRENT.md` — resume snapshot.
- `docs/DECISIONS.md` — D-001…D-010 (Python, local-only, read-only old repo, PDF-from-md, D2 verbatim, D3 real run, D5 genesis adopt+5 invariants, env conventions).
- `journal/2026-08-08-session.md` — (this file).
PLAN/RESUME/AGENTS/README/.gitignore — to be written next in this pass.

### 13:38 — Environment cross-checks (for commands in docs)
- `python` on PATH → 3.11 (ModuleNotFoundError pypdf); pythoncore-3.14 verified OK.
- scikit-learn/numpy NOT installed (Phase 3 plan: `.venv`).
- Chrome path verified: `C:\Program Files\Google\Chrome\Application\chrome.exe` v151.
- Git Bash verified; WSL not usable (`bash` has no distro).

### 13:42 — Status
- Phase 0 in progress; no commits yet. Continuity checkpoint files being finalized.

---

## Open commitments / for next session
1. **Phase 2**: copy old `research/**` verbatim; write `sources_catalog.md`, `README.md`.
2. **Phase 3**: `.venv` + scikit-learn/numpy; modify/run naive baseline; produce CSVs/JSONL/summary + `baseline_protocol.md` + `productive_failure_report.pdf`.
3. **Phase 4**: `.genesis` adoption/adaptation (Python re-slice), reset CURRENT/KICKOFF; add invariants #4 #5.
4. **Drive**: gates — evidence tags on all deliverables; one commit per phase; no placeholders; UI/app deferred.

---

## Continuation — 14:05

### 14:05 — Phase 0 completed (build mode resumed)
- Ported `C:\Users\CR7\AppData\Local\Temp\opencode\build_pdfs.py` → `tools/build_pdfs.py` (repo-relative REPO path, `smoke` subcommand). Pipeline verified: `smoke.pdf` built OK (24,076 bytes). Fixed OUT_HTML dir creation bug before smoke.
- No separate `chore: bootstrap` commit: checkpoint + tooling folded per single-commit decision.

### 14:10 — Phase 1 (D1 merge) executed
- Copied `D:\Abhii\Opencode\reconstruction\{01_problem,02_timeline,03_failure_analysis,04_first_principles,sources}.md` → `reconstruction\` (later renamed to handbook canonical names `failure_analysis.md` / `first_principles.md`).
- Grafts from old repo (read-only, `reconstruction/failure_analysis.md` + `first_principles.md`):
  - `01_problem.md` §5: added **Personal observation** block (3 `[O]` observations: loss of long-term project context; contradictory guidance; repeated preference specification).
  - `first_principles.md` §9: added **Stage → requirement mapping** table (Stage 0→R1/R3 … Stage 3→R5–R8).
  - `first_principles.md` §10: added open question **OQ9 — user memory controls** (inspect/edit/delete/disable memories; interaction with lifecycle).
- Regenerated PDFs from merged markdown: `problem_reconstruction.pdf` 6 pages (180,014 B), `historical_timeline.pdf` 4 pages (121,289 B). Text-verified: personal-observation content present in problem PDF; page counts stable (6/4).
- Commit: `feat(d1): merged reconstruction` → **2823f40** (8 files, +1167).

### 14:15 — Checkpoint updated
- CURRENT.md, SESSION_STATE.md updated to Phase 2; journal appended (this section).

### Next actions (recorded)
1. Phase 2: copy old repo `research/**` verbatim → `research/`; add `sources_catalog.md` + `README.md`; commit `feat(d2): research weeks 1-4 + sources catalog`.
2. Phase 3: `.venv` (scikit-learn, numpy), run naive baseline, reports + PDFs.
3. Phase 4: genesis spine adoption (Python re-slice, invariants ×5).

---

## Environment notes (jotted here for continuity)
- Temp scratch dir: `C:\Users\CR7\AppData\Local\Temp\opencode` (exists).
- Handbook extract: `C:\Users\CR7\AppData\Local\Temp\opencode\deliverable1.txt` (22 pages).
- Genesis kit (scaffold templates): `C:\Users\CR7\Desktop\genesis-kit`.
- PDF pipeline: `pythoncore-3.14\python.exe` + Chrome `--headless --print-to-pdf` (build script ported to `tools/build_pdfs.py`).

---

## Continuation — 14:20 onward (Phases 2–6)

### Phase 2 — D2 research copy (`feat(d2)`, commit `5f9677e`)
- Copied `D:\Abhii\Projects\Conversational-Memory-Intelligence-System-\research\**` verbatim → `research\`.
- Verified **18 files byte-identical** (get-filehash); fixed accidental `research/research/` nesting produced during copy.
- Added `research/sources_catalog.md` (field map: Mem0, Zep/Graphiti, MemGPT/Letta, Cognee, LangMem, MIRIX, A-MEM, Supermemory, OWASP ASI06, PIIBench, MemoryGraft, MINJA, Cisco MemoryTrap, pgvector, FAISS, HNSW, RRF, Braintrust, OpenObserve) + `research/README.md`.
- UTF-8 sanitized (0 U+FFFD remain).

### Phase 3 — D3 naive baseline RUN (`feat(d3)`, commit `1ff47a5`)
- Created `.venv` (scikit-learn 1.9.0, numpy 2.5.1) at repo root; installed via pythoncore-3.14.
- **First real run** of old-repo naive baseline; artifacts committed: `baseline_results.csv`, `error_examples.jsonl`, `summary.json`, `baseline_protocol.md`, `assumption_to_result.md`, `productive_failure_report.md` (→ PDF, 3 pages), `requirements.txt`.
- Repro check: rerun diff = timing keys only.
- Headline (falsification): recall@5=1.0, precision@1=0.857, contradiction_failure_rate=0.33, cold_start_false_positive_rate=0.5, sensitive_leak_rate_retrieval=1.0, sensitive_leak_rate_injection=1.0, task_level_pass_rate=0.67, latency p50≈0.74ms, p95≈0.84ms.

### Phase 4 — Genesis spine (`chore(genesis)`, commit `aaf37f0`)
- Copied old-repo `.genesis/` spine; re-sliced M1–M3 to `pytest tests/test_*.py -q` with freeze boundaries `src/memory_os/**`; `DONE.html` gates updated; MAPPING + README.
- Invariants now **5**: tenant_isolation, privacy_deletion_guarantee, retrieval_performance<150ms, token_budget_per_turn, pii_pre_post_guardrail.
- Approach B chosen (relational + pgvector, deterministic supersession via valid_until).

### Phase 5 — Product layer (`docs(product)`, commit `ed7ac56`)

- `product/PRD.md`: vision, personas, R1–R8→capabilities table, OQ9 user controls, competitive context (Mem0/Zep/Letta), markets.
- `design/edge_cases.md`: 18 rows EC-01…EC-18 (trigger/expected/capability/D6-test).

### Phase 6 — Research verification passes (THIS check-in, commit `docs(research)`)
- R1 — citation & claim verification (all `[P:n]` vs arXiv, full-text where claim-text):
  - 11/11 citations correct (P:11 = arXiv 2016 / TPAMI 2018 nuance).
  - Issues found & fixed (in repo + pass file `2026-08-08-R1-citation-verification.md`):
    - "~40% dense queries fail" **NOT in RAG paper** — practitioner-origin (Supermemory-guide 2026-04, `turion.ai`, `lushbinary`); re-anchored claims to `[P:4]`+`[O]`; number reframed as `[O] practitioner` where kept.
    - Generative Agents "consolidation" — not the paper's term; docs now say observation→reflection→summarization; "error reinforcement" re-framed as recognised follow-on concern (`[A]`), not a paper claim.
  - P-tag coverage: sources.md now includes the correction notes so a fresh grader can audit.
- R2 — D4 readiness (web, 2026-08-08): LoCoMo ACL-2024 (SOTA real numbers: mem-full-context 72.9% (full-context 72.9%), Mem0-graph 68.4%, Mem0 66.9%, RAG 61.0%, MIR 85.4%); Mem0 lifecycle = LLM-classified ADD/UPDATE/DELETE + `expiration_date` (hides, never deletes) vs our deterministic supersession; Letta v0.16.8 active; Zep/Graphiti bi-temporal supported; RRF (Cormack et al.) k=60 standard + 91% recall@10 hybrid figure = Supermemory hybrid-search blog (Apr 2026) not academic; pgvector HNSW scale numbers verified; **MIRIX corrected**: it's a memory-system paper (arXiv 2507.07957) + SOTA on LoCoMo, not "MENO".
- R3 — product positioning: vendor is **Supermemory** (not: Supertone is an unrelated Korean audio company); ecosystem: Mem0 $24M + AWS Agent SDK, OpenMemory sunset Jul-2026 + CVE-2026-59705, Zep active, LangMem active; OWASP LLM Top 10 (2025-edition) — **no ASI06/memory slot**; closest: LLM-04 (Data & Model Poisoning) and LLM-08 (Vector & Embedding Weakness). PRD updated.
- Repo edits applied: `reconstruction/sources.md` (claims table rewrite + comment), D1 docs re-anchored, `sources_catalog.md` rows verified/checked, `research/README.md` checkboxes checked, `product/PRD.md` R4 row corrected, `docs/SESSION_STATE.md` + `CURRENT.md` updated.
- Unverified item flagged: PIIBench OOD 0.96→0.18 figure remains editorial-only on catalog (from old repo research; D4-era verify target).

### Next actions (recorded)
1. Commit Phase 6 (`docs(research): verification passes`).
2. Phase 7: `journal/2026-08-08-merge.md` final merge journal (disclosure + decisions); end-of-merge checks: %PDF pipeline 6/4/3 pages, baseline rerun, placeholder scanner (%`INSERT`-style), dir diff vs old repo, handbook §F checklist; final acceptance report to user. Commit `docs(merge): final journal + checklist`.
3. Genesis M1-M3 become D6 build work (schema/tenant → hybrid RRF → admission/context) — only after D4 design, per handbook.

---

## Continuation — 19:20 onward (G-M1 build)

### Environment setup (before G-M1): portable Postgres 17 + pgvector
- No admin rights, no Docker, no WSL → portable route chosen:
  - EDB binaries zip `postgresql-17.10-2-windows-x64-binaries.zip` → `C:\Users\CR7\Postgres\17` (PostgreSQL 17.10).
  - pgvector 0.8.6 prebuilt for Windows (andreiramani/pgvector_pgsql_windows, release `0.8.6_17`) — conda-forge win-64 stale (≤0.4.0), no MSVC for source build.
  - `initdb -D C:\Users\CR7\Postgres\data -U memoryos --auth=trust --encoding=UTF8 --locale=C`; DB `memoryos`; `CREATE EXTENSION vector` → 0.8.6.
- Postmaster must run **detached via WMI** (`Invoke-CimMethod Win32_Process Create`): a `pg_ctl` started from the shell tool dies with `0xC0000142` when the tool session ends (job-object kills the process tree). Server now detached under parent cmd.exe (PID 13088), accepting connections.
- venv: `psycopg 3.3.4`, `sqlalchemy 2.0.51`, `pgvector`, `pytest` installed on Python 3.14 venv.

### G-M1 build (files under freeze boundary `src/memory_os/db/**`, `tests/`)
- `src/memory_os/db/schema.sql` — the canonical `memories` table, byte-matching `design/data_model.md` (id, tenant_id, user_id, text, dense_embedding vector(384), sparse_terms JSONB, admission_op/provenance/confidence/pii CHECKs, valid_from/valid_until, status, importance_score, consolidation_lineage UUID[], created_at/updated_at) + 5 indexes (tenant_status, HNSW dense, tenant_valid_from, lineage GIN, lifecycle_scan).
- `src/memory_os/db/store.py` — `MemoryStore`: connect (dict rows + pgvector adapter), apply_schema (idempotent, returns vector ext version), add, supersede (scoped UPDATE valid_until), delete (physical purge — invariant #2), get_active (tenant pre-filter), search_dense (tenant-scoped HNSW cosine), index_names. DSN from env `MEMORYOS_DB_DSN`, default `postgresql://memoryos@localhost:5432/memoryos`.
- `tests/test_db.py` — 12 tests: migration idempotency + all 5 indexes, tenant isolation (incl. adversarial near-duplicate wording across tenants), hard delete purges + tenant-scoped delete, valid_until nullable until superseded, HNSW nearest ranking, null-embedding exclusion, 8-thread concurrent writes.
- Fixes during build: dropped unused `psycopg.pool` import (extra not installed); schema loaded from `schema.sql` file (not a Python module); removed leftover python wrapper from schema.sql; test vector helper was ignoring its `n` argument (all embeddings identical → ranking test flaky) — fixed to shift pattern by `n`.

### G-M1 gate
- `pytest tests/test_db.py -q` → **12 passed in 2.19s**.
- Success criteria met: migration clean; tenant A never sees tenant B (deterministic pre-filter); DELETE physically removes rows; `valid_until` nullable until superseded; HNSW indexes per data_model.md.
- Edge cases covered: EC-01 (supersession storage), EC-03 (consent purge), EC-05 (cross-tenant adversarial similarity), EC-11 (concurrent writes serialize), EC-18 (no torn reads).
- Consistency note: `sprint_plan.md` line 40 mentions "episodic/semantic as a `memory_type` column per the harmonization note" — canonical `data_model.md` (D4, copied verbatim per D-015) has **no** `memory_type` column; the D-015 harmonization collapsed the kit's episodic/semantic split into the single `memories` table. `schema.sql` follows `data_model.md` exactly. (Recording here so the parenthetical doesn't get re-introduced.)
- Checkpoint: `.genesis/checkpoints/CURRENT.md` → active_loop BUILD, target G-M1, last_gate PASS.

### Next actions (recorded)
1. Commit G-M1 (`feat(g-m1): relational schema + tenant isolation`).
2. Open **G-M2** — hybrid retrieval: `src/memory_os/retrieval/**` + `src/memory_os/embeddings/**` (local 384-d embedder per ADR-007), BM25 (sparse_terms) + dense + RRF k=60, relevance floor → `no_relevant_memory`; D3's exact failing cases as tests. Gate: `pytest tests/test_retrieval.py -q`.
3. **G-M3** — admission (ADD/UPDATE/DELETE/NOOP, deterministic supersession) + context (zone budgets, PII guardrail). Gates: `pytest tests/test_admission.py -q`, `pytest tests/test_context.py -q`.

---

## Continuation — evening (G-M2 build)

### G-M2 embedder + retrieval stack (files under freeze boundary `src/memory_os/{embeddings,retrieval}/**`, `tests/`)
- Installed `sentence-transformers 5.7.0` + `torch 2.13.0` into `.venv` (Python 3.14); downloaded `all-MiniLM-L6-v2` (384-d, normalized, dot=cosine) into repo `.hf-cache/` (HF_HOME default in `embedder.py`; `.gitignore`d).
- `src/memory_os/embeddings/embedder.py` — lazy thread-locked load, `is_available()` false → BM25-only fallback (part3 §16); embedding generated at write time in `store.add`/test helpers.
- `src/memory_os/retrieval/tokenizer.py` — lowercase alnum + stopwords, `term_frequencies` for write-time `sparse_terms`, `numeric_tokens` (EC-16 exact slots).
- `src/memory_os/retrieval/bm25.py` — Okapi BM25 scores over tenant-scoped corpus stats (df, avg-dl).
- `src/memory_os/retrieval/rrf.py` — RRF k=60 fusion.
- `src/memory_os/retrieval/hybrid.py` — `HybridRetriever.search`: tenant-prefiltered dense (HNSW) + BM25 → RRF → relevance floor → explicit `NoRelevantMemory` (never an error). Two-signal floor: cosine ≥ 0.5 **AND** ≥2 shared lexical terms, OR paraphrase carve-out cosine ≥ 0.75; BM25-only fallback when dense unavailable (≤ 1 torch thread `torch.set_num_threads`).
- Calibration on D3's exact strings: keep c1 0.657, c2 0.731, c4-farah 0.557, ec16 0.945, aws 0.867; drop c6-lunch 0.426, c6-docs 0.517, c4-george 0.236, ec16-90 0.721. **c1's stale row scores 0.876 — higher than current truth** → supersession (valid_until filter), not scoring, is proven to close c1.
- `tests/test_retrieval.py` — 14 tests: D3 exact strings (c1 stale excluded via supersedes, c6 floor, c4 tenant isolation incl. paraphrase, ec16 numeric slot), RRF/BM25 unit tests, sparse-only offline path.
- Fixes during build: `psycopg cannot adapt dict` → wrap `sparse_terms` in `Jsonb`; `KeyError 'bm25'` → attach BM25 score into rich rows; PowerShell `-replace` mangles UTF-8 (â€™ etc.) — reverted to edit-tool.
- Corrections: the first latency numbers (p50 97ms, p95 206ms) were on a tenant whose rows had **NULL** dense/sparse (seeded before G-M2 columns); re-seeded with faithful d/h embeddings.

### EC-15 latency gate + perf work
- Root cause of the 206ms p95: **psycopg3's `Connection.__exit__` closes the connection on context exit** (`connection.py:155-176`) → every query paid a fresh TCP+auth connect (~30ms) *and* the public `connect()` reopened every call — reproduced as `connect` 30.9ms / `dense` 37ms / `bm25` 32ms in probes.
- Fix: `MemoryStore.session()` context manager — **persistent store-scoped connection**, commits/rolls back but never closes; `connect()` stays the fresh-connection API. All hot paths (`add`, `supersede`, `delete`, `get_active`, `search_dense`, hybrid `_bm25_rank`) now use `session()`.
- New: `tests/test_latency.py` (marker `-m latency`, registered in `pytest.ini`) — seeds 500 rows with real embeddings on dedicated tenant `latency-gate`, runs 40 queries, asserts **p95 < 150 ms**; teardown deletes the tenant. Verified: p95 ≈ 20 ms (p50 ≈ 14 ms).
- `bench/latency_profile.py` — human-run per-phase profiler (embed/dense/bm25/fuse), `--rows --queries`.
- Sprint risk #4 (pre-filtered HNSW): EXPLAIN at 500 rows → planner picks `idx_memories_tenant_status` + top-N heapsort (correct at that size); at 20k rows → **`Index Scan using idx_memories_dense` (HNSW), 0.17 ms**, filter pushed via `tenant_id`/`user_id`. Risk closed (planner escalates to HNSW at scale automatically).

### G-M2 gate
- `pytest tests/test_db.py test_retrieval.py test_latency.py -q` → **27 passed** (12 + 14 + 1). Full suite `pytest -q` → 27 passed in ~17s.
- Setup doc updated (`docs/SETUP_AND_RUN.md`) — sentence-transformers install + model download note, G-M2 gates table, latency benchmark section; `requirements.txt` pinned `sentence-transformers==5.7.0`, `torch==2.13.0`.
- Tenants `load`, `risk4`, `risk4-20k`, `a` cleared (scratch corpus, 20562 rows purged); DB back to empty.
- Commits: G-M2 lands as `feat(g-m2): hybrid retrieval ...`.
# Session Journal — 2026-08-09

> Chronological record: what was attempted, changed, succeeded/failed, commands, decisions, commits, next actions. Timestamps local (Asia/Kolkata). Covers the G-M3 build (admission + context) on top of G-M2 (bff78f9).

## 2026-08-09

### ~00:00 — G-M3 kicked off (from prior session)
- ADR-008 written (`design/decision_records/ADR-008-entity-slot-linking.md`) as the sprint_plan risk-#2 prerequisite for G-M3: deterministic utterance→slot grammar, correction markers → UPDATE (supersede), ADD fallback; revisit criteria recorded.
- `admission/patterns.py`: PII rule table (`[EMAIL]`, `[PHONE]`, `[SSN]`, `[CARD]`, `[API_KEY]`, `[IP]`, `[REDACTED]`), `scrub_pii`, `parse_delete_target`, `is_noop` (EC-017, EC-09 stopword-only), correction markers, `slot_key`.
- Probes green: `'password is hunter2'` → redacted, email redacted, date/IP safe, `'forget all the taco'` → target `taco`, `'hmm ok'` → NOOP, slot convergence (favorite coffee/food stay apart; tool switches converge).

### 01:30 — MemoryTrap detection (EC-06) added
- `is_instruction_like` in patterns; stored as data only — no mechanism executes memory text.

### 02:00 — Admitter + context builder tests written; three bugs found & fixed
1. **UPDATE supersession missing for "is on X"** — `slot_key('Alice is on Python and Flask')` was `None`, so the second turn was ADD, both rows stayed active and the correction loop leaked. Fix: **trailing slot rule** `\b(?:is\s+on|runs?\s+on|running\s+on|works?\s+on|migrated\s+(?:to|from)?)\s+` → `tool:choice`, placed AFTER the project/event rules so "the deadline is on Friday"/"the meeting is on Tuesday" still hit their specific slots (EC-02 no false collision).
2. **DELETE branch crashed** — `",".join(ids)` over UUIDs → `TypeError`; fixed with `str(i)`.
3. **`ContextBudget.from_budget_map` rejected partial maps** — explicit `zone_budgets` now carve out of the *remaining* budget; sum ≤ `token_budget` still enforced (api_contracts Endpoint 2). Exposed by the D3 c3 40-token case: default weights gave `retrieved_memory` only 16 tokens < one line.
- `MemoryStore.add` is keyword-only — tests updated (positional call crashed).

### 03:10 — Full suite green
- `pytest -q` → **54 passed**; `SELECT COUNT(*) FROM memories` → 0 (autouse `clean` now deletes after `yield`, mirroring `clean_table` in test_db).

## Decisions taken
- Slot grammar evaluated in listed order; generic `is on X` tool rule LAST so specific slots win (deadline/meeting/payment…).
- Explicit `zone_budgets` carve the remainder across defaults (weights renormalized) — necessary for D3's 40-token memory-zone exam case.
- Tests establish DB-empty end state; no manual cleanup between gate runs.

## Next actions
- Commit `feat(g-m3): deterministic admission + context gates` (pending per IMP protocol — user approval awaited for the checkpoint commit).
- Then open G-M4 per `design/sprint_plan.md`.

## GP-3 milestone map
G-M1 (storage–IO) committed d2d8e02 · G-M2 (hybrid retrieval) committed bff78f9 · G-M3 (admission+context) implemented, 54 green, commit pending.
# D6 implementation lives here when the build loops start (G-M1..G-M3).

See `.genesis/PLAN.md` for milestones and `design/sprint_plan.md` for the
demo commands and acceptance gates. Nothing committed yet.
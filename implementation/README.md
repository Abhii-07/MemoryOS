# D6 implementation

The MemoryOS implementation (src, tests, bench, audit policy, venv, model
cache) lives in **`implementation/MemoryOS-App/`** — see its
[`README.md`](MemoryOS-App/README.md) for layout and quick start.

See `.genesis/PLAN.md` for milestones and `design/sprint_plan.md` for the
demo commands and acceptance gates.